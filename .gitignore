# State files written by the skill at runtime — never committed
.buildproven-build/

# here-now claim tokens for deployed lesson artifacts — never committed
.herenow/

# Editor / OS noise
.DS_Store
*.swp
.vscode/
.idea/

# Local notes
NOTES.local.md
